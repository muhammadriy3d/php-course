<?php
  /* not inbetween
  $x = 4;
  if (!($x >= 1 and $x <= 4))
    echo "Hi!";
  */
  
  $x = 9;
  $y = 7;
  
  // If x is (3, 6, 9, 11)
  
    // print hello
    
  if ($x === 3 || $x === 6 || $x === 9 || $x === 11)
    echo "Hello";
  
  if (($x + $y) > 10)
    echo "hello";
  
  if (($x % 5) === 0)
    echo "pass";
  
  if (($x % 2) !== 0)
    echo "odd";
  
?>
